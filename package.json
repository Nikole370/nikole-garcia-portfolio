{
  "name": "nikole-garcia-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website of Nikole Garcia Chavez — Data Analytics, BI and junior Data Engineering",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --experimental-strip-types --test \"tests/*.test.ts\"",
    "verify": "npm run check && npm run test && npm run build"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.0"
  }
}
