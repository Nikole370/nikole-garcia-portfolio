# CV placeholder

Place the final CV here as `Nikole_Garcia_CV.pdf`.

Then set `cv.available: true` in `src/config/site.ts` — the "Download CV"
button in the hero appears automatically. While `available` is `false`, no
CV link is rendered anywhere (no broken links are published).

This README is never linked from the site; it only documents the placeholder.
